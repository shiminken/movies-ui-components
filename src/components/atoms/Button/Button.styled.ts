import styled from "@emotion/styled";
import colors from "../../../constants/colors";


export const ButtonWrapper = styled.button`
  border-radius: 5px;
  background-color: ${colors.darkGreen1};
  color: ${colors.white};
	border: none;
	padding: 10px;
  cursor: pointer;
`
